import React, { Component } from 'react';
import { StyleSheet, Text, View } from 'react-native';

export default class PlatformExamples extends Component {
  render() {
    return (
      <View>
          <Text>Selam</Text>
      </View>
    );
  }
}

const styles = StyleSheet.create({});